[project]
name = "rlm-mcp"
version = "0.1.0"
description = "Python bridge for rlm-mcp"
requires-python = ">=3.12"
dependencies = [
    "rlms",
    "openai",
    "pydantic",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "pytest-mock",
]
